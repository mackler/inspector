This is a simple project for inspecting an expression.
Just edit the `Main.scala` faile and replace the string with your code.
Then compile (don’t run) and the representation will appear in standard output.
